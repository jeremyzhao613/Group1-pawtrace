# Source Assets

Original media files kept for reference or future asset generation.

## Layout

- `pet-pictures/`: original pet photos, normalized to `pet-<number>.png`.
- `head-portraits/`: original portrait images, normalized to `person-<number>.png`.

Runtime-served app assets live one level up in `assets/` and in
`frontend/public/assets/`.
