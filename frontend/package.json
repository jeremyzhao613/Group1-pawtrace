{
  "name": "pawtrace-frontend",
  "private": true,
  "version": "5.2.0",
  "type": "module",
  "scripts": {
    "build:app-css": "tailwindcss -c ./tailwind.config.js -i ./public/app/style.tailwind.css -o ./public/app/app.css --minify",
    "dev": "npm run build:app-css && vite --host 0.0.0.0 --port 5173",
    "build": "npm run build:app-css && vite build",
    "preview": "vite preview",
    "cap:sync": "npx cap sync",
    "cap:sync:android": "npx cap sync android",
    "cap:sync:ios": "npx cap sync ios",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios",
    "build:monitor": "tailwindcss -c ../monitor/tailwind.config.cjs -i ../monitor/input.css -o ../monitor/assets/monitor.css",
    "build:all": "npm run build && npm run build:monitor"
  },
  "dependencies": {
    "@capacitor/core": "^8.3.0"
  },
  "devDependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/ios": "^8.3.0",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "~5.7.2",
    "vite": "^5.4.11"
  }
}
